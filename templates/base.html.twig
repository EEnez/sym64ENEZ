<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
        <style>
            :root {
                
                --primary-color: #2d3047;    
                --secondary-color: #384b6d;  
                --accent-color: #4f9da6;     
                --success-color: #1b998b;    
                --warning-color: #d4b483;    
                --danger-color: #ff6b6b;     
                --info-color: #5b89a6;       
                
                
                --light-bg: #f7f9fc;         
                --dark-bg: #1f2937;          
                --text-primary: #2d3047;    
                --text-secondary: #64748b;   
                --border-color: #e2e8f0;     
                --hover-bg: #f1f5f9;         
                
                
                --gradient-primary: linear-gradient(135deg, #2d3047, #384b6d);
                --gradient-accent: linear-gradient(135deg, #4f9da6, #1b998b);
                --gradient-warning: linear-gradient(135deg, #d4b483, #c89b64);
                --gradient-danger: linear-gradient(135deg, #ff6b6b, #ff5252);
                
                
                --card-shadow: 0 4px 20px rgba(45, 48, 71, 0.08);
                --card-border-radius: 12px;
                --button-border-radius: 8px;
            }
            
            body {
                font-family: 'Inter', system-ui, -apple-system, sans-serif;
                background-color: var(--light-bg);
                color: var(--text-primary);
                line-height: 1.7;
            }
            
            .navbar {
                box-shadow: 0 2px 4px rgba(0,0,0,.1);
            }
            
            .card {
                border: none;
                border-radius: var(--card-border-radius);
                box-shadow: var(--card-shadow);
                transition: transform 0.3s ease, box-shadow 0.3s ease;
                background: #ffffff;
            }
            
            .card:hover {
                transform: translateY(-4px);
                box-shadow: 0 8px 25px rgba(43, 65, 98, 0.12);
            }
            
            .btn {
                padding: 0.625rem 1.5rem;
                border-radius: var(--button-border-radius);
                font-weight: 500;
                transition: all 0.3s ease;
                border: none;
            }
            
            .btn-primary {
                background: var(--gradient-primary);
                border: none;
            }
            
            .btn-outline-primary {
                border: 2px solid var(--primary-color);
                background: transparent;
            }
            
            .btn:hover {
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(43, 65, 98, 0.15);
            }
            
            h1, h2, h3 {
                color: var(--primary-color);
                font-weight: 600;
                letter-spacing: -0.02em;
            }
            
            .article-content {
                line-height: 1.8;
                font-size: 1.1rem;
            }
            
            .metadata {
                font-size: 0.9rem;
                color: #666;
            }
            
            .badge {
                padding: 0.5em 1em;
                border-radius: 6px;
                font-weight: 500;
                letter-spacing: 0.01em;
            }
            
            .badge:hover {
                transform: scale(1.05);
            }
            
            .alert {
                border: none;
                border-radius: var(--card-border-radius);
                padding: 1rem 1.25rem;
                border-left: 4px solid transparent;
            }
            
            .alert-info {
                background: rgba(18, 181, 229, 0.1);
                border-left-color: var(--accent-color);
            }
            
            .alert-success {
                background: rgba(0, 166, 118, 0.1);
                border-left-color: var(--success-color);
            }
            
            .form-control {
                border-radius: var(--button-border-radius);
                border: 1px solid var(--border-color);
                padding: 0.75rem 1rem;
                transition: all 0.2s ease;
            }
            
            .form-control:focus {
                border-color: var(--accent-color);
                box-shadow: 0 0 0 3px rgba(18, 181, 229, 0.1);
            }
            
            .section-header {
                position: relative;
                margin-bottom: 3rem;
                text-align: center;
            }
            
            .section-header:after {
                content: '';
                position: absolute;
                bottom: -1rem;
                left: 50%;
                transform: translateX(-50%);
                width: 60px;
                height: 4px;
                background: var(--accent-color);
                border-radius: 2px;
            }
            
            .metadata a {
                color: var(--primary-color);
                font-weight: 500;
            }
            
            .metadata a:hover {
                color: var(--accent-color);
            }
            
            .table {
                border-collapse: separate;
                border-spacing: 0;
            }
            
            .table th {
                background: var(--light-bg);
                font-weight: 600;
                padding: 1rem;
            }
            
            .table td {
                padding: 1rem;
                vertical-align: middle;
            }
            
            .comment-form {
                background: var(--light-bg);
                border-radius: var(--card-border-radius);
                padding: 1.5rem;
                margin-bottom: 2rem;
            }
            
            .comment-form textarea.form-control {
                min-height: 120px;
                background: #fff;
                border: 1px solid var(--border-color);
                padding: 1rem;
                font-size: 1rem;
                line-height: 1.6;
                resize: vertical;
            }
            
            .comment-form textarea.form-control:focus {
                border-color: var(--accent-color);
                box-shadow: 0 0 0 3px rgba(18, 181, 229, 0.1);
            }
            
            /* Bootstrap Color Overrides */
            .btn-accent {
                background: var(--gradient-accent);
                border: none;
                color: white;
            }
            
            .btn-warning {
                background: var(--gradient-warning);
                border: none;
                color: white;
            }
            
            .btn-danger {
                background: var(--gradient-danger);
                border: none;
            }
            
            .badge.bg-primary {
                background: var(--gradient-primary) !important;
            }
            
            .badge.bg-secondary {
                background: var(--accent-color) !important;
            }
            
            .badge.bg-success {
                background: var(--success-color) !important;
            }
            
            .badge.bg-warning {
                background: var(--warning-color) !important;
                color: var(--dark-bg);
            }
            
            .badge.bg-danger {
                background: var(--danger-color) !important;
            }
            
            .badge.bg-info {
                background: var(--info-color) !important;
            }
            
            /* Form Controls */
            .form-control:focus {
                border-color: var(--accent-color);
                box-shadow: 0 0 0 0.2rem rgba(79, 157, 166, 0.15);
            }
            
            .form-check-input:checked {
                background-color: var(--accent-color);
                border-color: var(--accent-color);
            }
            
            /* Alert Styles */
            .alert-primary {
                background: rgba(45, 48, 71, 0.1);
                border-left: 4px solid var(--primary-color);
                color: var(--primary-color);
            }
            
            .alert-info {
                background: rgba(91, 137, 166, 0.1);
                border-left: 4px solid var(--info-color);
                color: var(--info-color);
            }
            
            .alert-success {
                background: rgba(27, 153, 139, 0.1);
                border-left: 4px solid var(--success-color);
                color: var(--success-color);
            }
            
            .alert-warning {
                background: rgba(212, 180, 131, 0.1);
                border-left: 4px solid var(--warning-color);
                color: var(--dark-bg);
            }
            
            .alert-danger {
                background: rgba(255, 107, 107, 0.1);
                border-left: 4px solid var(--danger-color);
                color: var(--danger-color);
            }
        </style>
    </head>
    <body>
        {% include 'partials/_nav.html.twig' %}
        
        <div class="container mt-4">
            {% block body %}{% endblock %}
        </div>
        
        <footer class="bg-light mt-5 py-4">
            <div class="container text-center">
                <p class="text-muted mb-0">© {{ "now"|date("Y") }} SYM-ENEZ - All rights reserved</p>
            </div>
        </footer>
        
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
